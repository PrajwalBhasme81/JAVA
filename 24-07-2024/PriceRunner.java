public class PriceRunner
{
	public static void main(String args[])
	{
		Price amount = new Price();
		
		amount.getLengthOfArray();
		amount.createArray("5000");
		amount.createArray("2500");
		amount.createArray("15000");
		amount.createArray("1000");
		amount.readArray();
	}
}